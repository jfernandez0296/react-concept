import React from 'react';
import './Loader.scss'; // Asegúrate de que este archivo CSS exista en la misma carpeta

const Loader = () => {
  return (
    <div className="loader-container">
      <div className="loader"></div>
    </div>
  );
};

export default Loader;
